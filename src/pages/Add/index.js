/* Modules */
import React from 'react';
import Firebase from '../../firebase';

const Add = props => {
	return (
		<React.Fragment>
			<h1>Add: Tracker-man</h1>
		</React.Fragment>
	);
};

export default Add;
