/* Modules */
import React from 'react';
import Firebase from '../../firebase';

const Profile = props => {
	return (
		<React.Fragment>
			<h1>Profile: Tracker-man</h1>
		</React.Fragment>
	);
};

export default Profile;
