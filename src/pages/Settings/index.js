/* Modules */
import React from 'react';
import Firebase from '../../firebase';

const Settings = props => {
	return (
		<React.Fragment>
			<h1>Settings: Tracker-man</h1>
		</React.Fragment>
	);
};

export default Settings;
